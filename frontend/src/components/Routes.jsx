import{Navigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';
export function ProtectedRoute({children}){const{user,loading}=useAuth();if(loading)return <div className="p-24">Loading...</div>;return user?children:<Navigate to="/login"/>}export function AdminRoute({children}){const{user,loading}=useAuth();if(loading)return <div className="p-24">Loading...</div>;return user?.role==='ADMIN'?children:<Navigate to="/login"/>}

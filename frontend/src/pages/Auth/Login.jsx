import{useState}from'react';import{useNavigate,Link}from'react-router-dom';import{useAuth}from'../../context/AuthContext';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),{login}=useAuth(),nav=useNavigate();return <div className="max-w-md mx-auto py-24"><div className="glass-panel p-8 rounded-3xl"><h1 className="text-3xl font-bold mb-6">Login</h1>{err&&<p className="text-red-400">{err}</p>}<input className="w-full mb-3 p-3 bg-black/50 rounded-xl" placeholder="Email" onChange={e=>setEmail(e.target.value)}/><input className="w-full mb-3 p-3 bg-black/50 rounded-xl" type="password" placeholder="Password" onChange={e=>setPassword(e.target.value)}/><button className="w-full bg-white text-black p-3 rounded-xl" onClick={async()=>{try{await login({email,password});nav('/dashboard')}catch(e){setErr(e.message)}}}>Login</button><button className="w-full mt-3 border border-white/10 p-3 rounded-xl">Google sign-in</button><Link className="text-brand-400 block mt-4" to="/register">Create account</Link></div></div>}

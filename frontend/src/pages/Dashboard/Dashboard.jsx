import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{api}from'../../api/client';export default function Dashboard(){const[servers,setServers]=useState([]);useEffect(()=>{api('/api/servers').then(d=>setServers(d.servers))},[]);return <section className="max-w-7xl mx-auto px-6 py-16"><h1 className="text-4xl font-bold mb-8">Your Servers</h1><div className="grid md:grid-cols-3 gap-6">{servers.map(s=><Link to={`/dashboard/servers/${s.id}`} key={s.id} className="glass-panel p-6 rounded-2xl"><h3 className="text-xl font-bold">{s.name}</h3><p className="text-brand-400">{s.status}</p><p className="text-slate-400">{s.plan.name}</p></Link>)}</div></section>}

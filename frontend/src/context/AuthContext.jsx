import React,{createContext,useContext,useEffect,useState}from'react';import{api}from'../api/client';
const C=createContext(null);export function AuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{api('/api/auth/me').then(d=>setUser(d.user)).catch(()=>setUser(null)).finally(()=>setLoading(false))},[]);const login=async(body)=>{const d=await api('/api/auth/login',{method:'POST',body});setUser(d.user)};const register=async(body)=>{const d=await api('/api/auth/register',{method:'POST',body});setUser(d.user)};const logout=async()=>{await api('/api/auth/logout',{method:'POST'});setUser(null)};return <C.Provider value={{user,setUser,loading,login,register,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);

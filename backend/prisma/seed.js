require('dotenv').config(); const bcrypt=require('bcryptjs'); const {PrismaClient}=require('@prisma/client'); const prisma=new PrismaClient();
async function main(){const plans=[['starter','Starter',300,2048,10240,100,1,7,['US-East'],false],['pro','Pro',800,6144,30720,200,3,30,['US-East','EU-West','AP-Singapore'],true],['extreme','Extreme',1600,16384,102400,400,8,60,['US-East','EU-West','Frankfurt','Singapore','Sydney','US-West','London','NYC'],false]]; for(const [slug,name,priceCents,ramMb,diskMb,cpuPercent,maxDatabases,maxBackups,locations,isFeatured] of plans) await prisma.plan.upsert({where:{slug},update:{name,priceCents,ramMb,diskMb,cpuPercent,maxDatabases,maxBackups,locations,isFeatured,isActive:true},create:{slug,name,priceCents,ramMb,diskMb,cpuPercent,maxDatabases,maxBackups,locations,isFeatured}}); const email=process.env.ADMIN_EMAIL||'admin@mpanel.local', password=process.env.ADMIN_PASSWORD||'admin12345'; const existing=await prisma.user.findUnique({where:{email}}); if(!existing){await prisma.user.create({data:{email,name:'MPanel Admin',passwordHash:await bcrypt.hash(password,12),role:'ADMIN',authProvider:'LOCAL'}}); console.log(`Admin created: ${email} / ${password}`);} console.log('Seed complete');}
main().finally(()=>prisma.$disconnect());
